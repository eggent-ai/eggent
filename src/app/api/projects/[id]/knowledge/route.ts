export async function GET() {
  return Response.json(
    { error: "Project RAG/knowledge has been removed. Use project files, memory.md, skills/, and pipeline artifacts instead." },
    { status: 410 }
  );
}

export async function POST() {
  return Response.json(
    { error: "Project RAG/knowledge has been removed. Use project files, memory.md, skills/, and pipeline artifacts instead." },
    { status: 410 }
  );
}

export async function DELETE() {
  return Response.json(
    { error: "Project RAG/knowledge has been removed. Use project files, memory.md, skills/, and pipeline artifacts instead." },
    { status: 410 }
  );
}
